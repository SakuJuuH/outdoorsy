package com.example.outdoorsy.ui.theme

import androidx.compose.ui.graphics.Color

val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)


// --- Theme: Forest Explorer ---

// Primary & Accent Colors
val forestGreen = Color(0xFF2d6a4f) // A deep, trustworthy green for primary buttons and highlights.
val pineGreen = Color(0xFF40916c)   // A slightly lighter green for secondary elements.
val accentGold = Color(0xFFffb703)  // A vibrant gold for special accents, like sale prices.

// Neutral & Background Colors
val offWhite = Color(0xFFF7F7F7)   // A soft white for the main background.
val cardBackground = Color(0xFFFFFFFF) // Pure white for cards to make them pop.
val stoneGray = Color(0xFFE0E0E0)   // A light gray for dividers and placeholders.
val textPrimary = Color(0xFF1b4332) // A very dark green for primary text, easier on the eyes than black.
val appBackground = Color(0xFFC5D3C5) // A softer green for secondary text and captions.
