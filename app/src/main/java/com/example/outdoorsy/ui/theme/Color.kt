package com.example.outdoorsy.ui.theme

import androidx.compose.ui.graphics.Color

// --- Theme: Forest Explorer ---

// Primary & Accent Colors

// A deep, trustworthy green for primary buttons and highlights.
val forestGreen = Color(0xFF2d6a4f)

// A slightly lighter green for secondary elements.
val pineGreen = Color(0xFF40916c)

// A darker, more saturated gold for better contrast
val accentGold = Color(0xFFd99000)

// Neutral & Background Colors

// A lighter background for better text contrast
val offWhite = Color(0xFFFAFAFA)

// Pure white for cards to make them pop.
val cardBackground = Color(0xFFFFFFFF)

// A medium gray for dividers and placeholders
val stoneGray = Color(0xFFBDBDBD)

// Darker text for better contrast on light backgrounds
val textPrimary = Color(0xFF0d1f15)

// Secondary text - darker for better readability
val textSecondary = Color(0xFF1b4332)

// Light background with better contrast potential
val appBackgroundLight = Color(0xFFE8F0E8)

// A darker green for the main background
val appBackgroundDark = Color(0xFF0a1710)

// Additional colors for better contrast in dark mode
val textOnDarkPrimary = Color(0xFFFFFFFF)
val textOnDarkSecondary = Color(0xFFE0E0E0)
